language: php
php:
  - 7.0
  - 7.1

node_js:
  - 'stable'

before_script:
  - composer install --dev --no-interaction --prefer-source

script:
  - composer lint
  - composer test
  - npm run lint
  - npm i && npm run test
